import { triggerHaptic, sendNudge } from '../../services/notificationService';\n\ndescribe('NotificationService', () => {\n  test('triggerHaptic does not crash', () => {\n    expect(() => triggerHaptic('light')).not.toThrow();\n  });\n\n  test('triggerHaptic handles different intensities', () => {\n    expect(() => triggerHaptic('medium')).not.toThrow();\n    expect(() => triggerHaptic('heavy')).not.toThrow();\n  });\n\n  test('sendNudge handles invalid input', async () => {\n    const result = await sendNudge('', '');\n    expect(typeof result).toBe('boolean');\n  });\n\n  test('sendNudge handles valid input', async () => {\n    const result = await sendNudge('Test message', 'test-context');\n    expect(typeof result).toBe('boolean');\n  });\n});